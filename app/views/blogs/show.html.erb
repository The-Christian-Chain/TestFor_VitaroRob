<p id="notice"><%= notice %></p>
<p>
  <strong>Name:</strong>
  <%= @blog.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @blog.description %>
</p>

<p>
  <strong>Has Sections?</strong>
  <%= Blog::SECTIONS.find {|item| item[1] == @blog.has_sections }[0] %>
</p>

<p>
  <strong>Allows Subscriptions?</strong>
  <%= Blog::SUBSCRIPTIONS.find {|item| item[1] == @blog.has_subscription }[0] %>
</p>

<p>
  <strong>Visible:</strong>
  <%= Blog::VISIBLE.find {|item| item[1] == @blog.visible }[0] %>
</p>

<%= link_to 'Edit', edit_blog_path(@blog) %> |
<%= link_to 'Back', blog_path %> | 
<%= link_to 'Delete', @blog, method: :delete, data: { confirm: 'Are you sure?' } %>